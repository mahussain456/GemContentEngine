# Claude Code local config (do not push)
.claude/

# Large video / binary assets that exceed GitHub's 100MB single-file limit
*.mp4
*.mov

# Python caches
__pycache__/
*.pyc
*.pyo

# OS / IDE
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Local secrets / env
.env
.env.*
!.env.example
*.pem
*.key
*.cert

# Temp / build
*.tmp
*.swp
*.swo
*~
.cache/
node_modules/
build/

# dist/ contains seller-side buyer-ZIP templates (tracked) — but ignore
# any per-order generated artifacts so we don't accidentally commit buyer data.
dist/LICENSE-*.txt

# Local working scratch
/tmp/
*.bak

# Generated buyer ZIPs (built by scripts/package_buyer_zip.py)
GEM-Content-Engine-v*.zip
